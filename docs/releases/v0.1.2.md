# spreadsheet-rescue v0.1.2

Release date: 2026-02-14

## Highlights

* Added deterministic dashboard preview rendering from `Final_Report.xlsx`:
  * new script: `scripts/render_dashboard_preview.py`
  * output artifact: `demo/dashboard.png`
* Extended `./scripts/demo.sh` to generate the preview PNG as part of the standard demo flow.
* Added regression tests for preview generation, asset validity, and error handling.

## Docs and demo

* Updated demo walkthrough to include preview rendering and manual renderer command.
* Updated README quickstart output and screenshot to use `demo/dashboard.png`.

## CI and quality

* Demo smoke checks now verify `demo/dashboard.png` exists.
* Local quality checks pass:
  * `uv run ruff check src tests`
  * `uv run mypy src/spreadsheet_rescue`
  * `uv run pytest -q`

## Upgrade notes

* No breaking CLI changes.
* Package version bumped to `0.1.2`.
